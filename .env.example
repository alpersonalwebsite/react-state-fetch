# Copy to .env and edit. .env is gitignored; only REACT_APP_-prefixed variables reach
# the bundle, and Create React App inlines them at build time, so never put a secret
# here.
#
# Defaults to the local API from alpersonalwebsite/node-express-postgresql.
REACT_APP_API_URL=http://localhost:3333/api/users
